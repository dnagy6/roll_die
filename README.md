# roll_die
Make a class Die with one attribute filled sides, which has a default value of 6. Write a method called ro11_die() that prints a random number between 1 and the number of sides the die has. Make a 6-sided die and roll it 10 times.
